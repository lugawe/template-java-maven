# template-java-maven

A simple template for java maven

## Build and run

Build jar with maven:

```bash
./mvnw clean package
```

Run jar with java:

```bash
java -jar target/template-java-maven-0.1.jar
```

## Run tests

Run tests

```bash
./mvnw test
```
